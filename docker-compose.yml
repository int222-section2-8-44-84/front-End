version: '3.0'
services:
    frontend:
        container_name: frontend
        build: 
          context: .
        image: frontend
        ports:
            - "80:80"
networks:
  default:
    external:
      name: wongnokproject